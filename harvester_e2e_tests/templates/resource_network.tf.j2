resource "harvester_network" "{{ name }}" {
  name      = "{{ name }}"
  namespace = "{{ net_namespace | default('default') }}"

  vlan_id = {{ vlan_id }} 

  lifecycle {
    ignore_changes = all
  }
}
