resource "harvester_network" "{{ name }}" {
  name      = "{{ name }}"
  namespace = "{{ net_namespace | default('harvester-public') }}"

  vlan_id = {{ vlan_id }} 

  lifecycle {
    ignore_changes = all
  }
}
